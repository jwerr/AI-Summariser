# --- Core web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database & ORM ---
SQLAlchemy==2.0.35
psycopg[binary]==3.2.3          # modern psycopg3 driver
pgvector==0.3.5

# --- Settings & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2        # if you want BaseSettings-style config
python-dotenv==1.0.1
email-validator==2.2.0

# --- HTTP clients & networking ---
httpx==0.27.2
requests==2.32.3

# --- File uploads / forms ---
python-multipart==0.0.12

# --- Auth, security, hashing ---
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
itsdangerous==2.2.0

# --- AI / LLM ---
openai==1.51.0                  # new style: from openai import OpenAI

# --- Google APIs (Calendar, OAuth) ---
google-api-python-client==2.154.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1

# --- Microsoft / Outlook / Teams auth (MSAL) ---
msal==1.31.0

# --- Background tasks / scheduling (optional) ---
APScheduler==3.11.0

# --- Email / templates (optional) ---
aiosmtplib==3.0.2
jinja2==3.1.4

# --- CORS & extras ---
starlette==0.40.0

# --- Dev tools & testing (optional but nice to have) ---
pytest==8.3.3
pytest-asyncio==0.24.0
black==24.10.0
isort==5.13.2
mypy==1.13.0
